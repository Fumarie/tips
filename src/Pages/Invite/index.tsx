import React from 'react';
import MainTitle from "../../Components/Main/MainTitle";

const Invite = () => {
    return (
        <div>
            <MainTitle text={'Invite friends'} />
        </div>
    );
};

export default Invite;
