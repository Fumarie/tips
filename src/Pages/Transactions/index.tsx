import React from 'react';
import MainTitle from "../../Components/Main/MainTitle";

const Transactions = () => {
    return (
        <div>
            <MainTitle text={'Transactions history'} />
        </div>
    );
};

export default Transactions;
